version: "3"

services:
  db:
    container_name: totem-backend-db
    image: postgres:14.7
    volumes:
      - db-data:/var/lib/postgresql/data
    ports:
      - '5432:5432'
    environment:
      - POSTGRES_DB=postgres
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
    command: ["postgres", "-c", "log_statement=all"]
    networks:
      - totem-backend-network

  django:
    container_name: totem-backend
    build:
      context: .
    command: "uvicorn totem.asgi:application --port 8000 --host 0.0.0.0 --reload --log-level warning"
    entrypoint: "docker-entrypoint-local.sh"
    volumes:
      - ./src:/usr/src/app
      - totem-static:/usr/src/static
      - totem-media:/usr/src/media
    ports:
      - "8000:8000"
    environment:
      - POSTGRES_NAME=postgres
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
    env_file:
      - default.env
    tty: true
    stdin_open: true
    depends_on:
      - db
    networks:
      - totem-backend-network
      - totem-saas-network

  nginx:
    container_name: totem-backend-nginx
    image: nginx
    depends_on:
      - django
    ports:
      - "8080:80"
    volumes:
      - totem-static:/usr/src/static
      - totem-media:/usr/src/media
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf
    networks:
      - totem-backend-network
      - totem-saas-network

volumes:
  db-data:
  totem-static:
  totem-media:

networks:
  totem-backend-network:
    name: totem-backend-network
  totem-saas-network:
    name: totem-saas-network
    external: true
